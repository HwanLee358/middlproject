<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wear.wishlist.mapper.WishListMapper">
  <select id = "selectwishlist" resultType = "com.wear.wishlist.vo.WishListVO" parameterType = "String">
  	select wishlist_no, p.product_no, user_id, product_img, product_name, product_price	
	from wishlist w left join product p on (w.product_no = p.product_no)
  	where user_id = #{userId}
  </select>
  
  <delete id = "deletewishList" parameterType = "int">
  	delete from wishlist
  	where wishlist_no = #{wishlistNo}
  </delete>
  
  <insert id = "insertWishlist" parameterType = "wishlist">
  	insert into wishlist(wishlist_no, product_no, user_id)
	values(wishlist_seq.nextval, #{productNo}, #{userId})
  </insert>
  <select id="getSelectWish" resultType="int" parameterType="wishlist">
  	select count(*) 
  	from wishlist
  	where user_id = #{userId}
  	and product_no = #{productNo}
  </select>
  <select id="sWishList" resultType = "com.wear.wishlist.vo.WishVO" parameterType="int">
  	select p.product_name, pi.product_color, pi.product_size, p.product_price, pi.product_info_no
	from product p left join product_info pi on (p.product_no = pi.product_no)
	where p.product_no = #{productInfoNo}
  </select>

</mapper>